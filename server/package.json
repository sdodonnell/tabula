{
  "name": "tabula-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "studio": "npx prisma studio",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm build && node dist/index.js",
    "dev:watch": "concurrently \"pnpm build --watch\" \"node --watch dist/index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.6",
    "concurrently": "^8.2.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@prisma/client": "5.4.2"
  },
  "prisma": {
    "seed": "npx ts-node prisma/seed.ts"
  }
}
